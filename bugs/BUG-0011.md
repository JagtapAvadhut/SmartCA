## BUG-0011

| Field | Value |
|-------|--------|
| **Title** | Practice / WM seed has no Receptionist in `@wm.smartca.in` org (reception only in demo USR seed) |
| **Severity** | Medium |
| **Priority** | P1 |
| **Steps** | Try `reception1@wm.smartca.in`; query roles. |
| **Expected** | Reception in practice org for intake desk UAT alongside Manager/CA/TL/Emp. |
| **Actual** | No WM reception user. Demo `manoj.malhotra@smartca.in` / `harsh.bose@smartca.in` (`receptionist`) work for intake API/UI, but outside WM 5k org. |
| **Screenshot** | `saas/qa-artifacts/qa-manual/reception_intake.png` |
| **Video** | N/A |
| **API evidence** | Reception login OK on demo user; intake create 201; work list total 0; assign/close 403. |
| **Database evidence** | `receptionist` count=2 (USR-*), zero in WM emails. |
| **Root Cause hypothesis** | `wmseed` only creates manager/ca/tl/employee. |
| **Status** | FIXED-PENDING-QA |
| **Fix (Developer)** | `wmseed` seeds `WM-RCP-0001/0002` (`reception1/2@wm.smartca.in`, role `reception`, `PermissionsForRole` intake-only, password `SmartCA@2025`, reportsTo first manager). One-shot `go run ./cmd/wmreceptionseed` upserts without wiping 5k works. `practiceuatseed` already has ROLE_HIERARCHY reception (Fatima/Leena). |
| **Verify** | Login `reception1@wm.smartca.in` → role=reception, `intake.create`, no assign/edit, work list total=0. |

---
