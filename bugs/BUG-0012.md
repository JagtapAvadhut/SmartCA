## BUG-0012

| Field | Value |
|-------|--------|
| **Title** | Senior CA / Junior CA / Accountant / Article demo users lack seeded work portfolio (total=0) |
| **Severity** | Medium |
| **Priority** | P2 |
| **Status** | **FIXED-PENDING-QA** |
| **Steps** | Login preferred Practice UAT emails (or demo aliases); `GET /work/items`. |
| **Expected** | Practice roles exerciseable with assigned works in UAT seed. |
| **Actual (original)** | Login OK; list **200 total=0**. JWT also lacks `work.view` (role middleware still allows empty list). Cannot UAT junior high-risk CA-verify rules with seed data. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence (original)** | `list_scope_senior_ca/junior_ca/accountant/article` total=0. |
| **API evidence (retest local)** | Practice: `vikram@…` total=**25**, `aditya@…` **19**, `ganesh@…` **17**, `kunal@…` **17**. Demo aliases: `suresh.gupta` / `rahul.joshi` / `arun.mehta` / `sanjay.verma` each total=**40**. |
| **Database evidence** | Original: no works with those actor ids in triad/assignee. After fix: senior_ca on `owner_ca_id`; junior/accountant/article on `assignee_id`. |
| **Root Cause hypothesis** | Demo USR roles not linked to WM seed works; Alok UAT seed missing senior_ca from owner portfolio (BUG-0005). |
| **Fix (Developer)** | `practiceuatseed`: ~20% of PRACTICE works use `senior_ca` as `owner_ca_id`; junior/accountant/article remain assignee rotation; `seedDemoRolePortfolios` creates 40 PRACTICE works per demo USR email when present. QA script prefers `@practice.smartca.in`. |
| **UAT emails (password `SmartCA@2025`)** | **Preferred:** `vikram@practice.smartca.in` (senior_ca), `aditya@practice.smartca.in` (junior_ca), `ganesh@practice.smartca.in` (accountant), `kunal@practice.smartca.in` (article). **Demo aliases:** `suresh.gupta@smartca.in`, `rahul.joshi@smartca.in`, `arun.mehta@smartca.in`, `sanjay.verma@smartca.in`. |
| **Run** | `cd Go && go run ./cmd/practiceuatseed` |
| **Regression** | `go test ./cmd/practiceuatseed/ ./internal/workmgmt/...` PASS |

### QA checklist

- [ ] Login preferred Practice emails → `GET /work/items` total > 0 for senior_ca / junior_ca / accountant / article
- [ ] Demo aliases also total > 0 (optional)
- [ ] `go test ./cmd/practiceuatseed/ ./internal/workmgmt/...` PASS

---
