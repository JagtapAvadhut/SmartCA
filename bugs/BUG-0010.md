## BUG-0010

| Field | Value |
|-------|--------|
| **Title** | Work create always requires `assignedTo`/`assigneeId` — cannot create unassigned OPEN |
| **Severity** | Medium |
| **Priority** | P1 |
| **Status** | **FIXED-PENDING-QA** |
| **Steps** | Manager `POST /work/items` with title + client only (or ITR without assignee). |
| **Expected** | Allow OPEN work after intake/engagement before assignee slot (triad assign APIs exist). |
| **Actual (original)** | **400** `assignedTo/assigneeId is required`. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence (original)** | `soft_low 400 {"message":"assignedTo/assigneeId is required"}`. |
| **Database evidence** | N/A |
| **Root Cause hypothesis** | Create validation still enforces legacy assignee-required rule incompatible with assign-slot workflow. |
| **Fix (Developer)** | `CreateWork` no longer requires `assignedTo`/`assigneeId`. Unassigned creates land as `OPEN` or `DOCUMENT_PENDING`; assign checks (`PermAssign` + `CanAssignTo`) only when an assignee is provided. Employees still lack `work.create`. Assignee can be set later via AssignSlot. Regression: `TestCreateWork_UnassignedOpen`. |
| **Regression** | `TestCreateWork_UnassignedOpen` + `go test ./internal/workmgmt/...` PASS |

### QA checklist

- [ ] Rebuild/restart API `:8080`
- [ ] Manager `POST /work/items` with title + client only (no assignee) → **201/200**, status `OPEN`, empty assignee
- [ ] CA create with `status: DOCUMENT_PENDING` and no assignee → succeeds
- [ ] Manager create **with** assignee still succeeds
- [ ] Employee create still rejected
- [ ] AssignSlot can fill assignee after unassigned create

---
