## BUG-0010

| Field | Value |
|-------|--------|
| **Title** | Work create always requires `assignedTo`/`assigneeId` — cannot create unassigned OPEN |
| **Severity** | Medium |
| **Priority** | P1 |
| **Status** | **VERIFIED** (2026-07-27) |
| **Steps** | Manager `POST /work/items` with title + client only (or ITR without assignee). |
| **Expected** | Allow OPEN work after intake/engagement before assignee slot (triad assign APIs exist). |
| **Actual (original)** | **400** `assignedTo/assigneeId is required`. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence (original)** | `soft_low 400 {"message":"assignedTo/assigneeId is required"}`. |
| **API evidence (retest)** | Rebuilt API `:8080`. Manager unassigned create **201** `status=OPEN`, empty `assignedTo`/`assigneeId` (`019fa3c1-b71c-7b4f-b809-4b516aac4806`). CA `DOCUMENT_PENDING` no assignee **201** (`019fa3c1-b74f-75c4-9e97-ac29318f854b`). Employee create **403**. Manager with assignee **201** (`019fa3c1-b784-77f5-b809-699daa3cd77c`). AssignSlot after unassigned **200** → `assigneeId=WM-EMP-0001`. Artifacts: `saas/qa-artifacts/qa-manual/bug0010-*.json`. |
| **Database evidence** | N/A |
| **Root Cause hypothesis** | Create validation still enforces legacy assignee-required rule incompatible with assign-slot workflow. |
| **Fix (Developer)** | `CreateWork` no longer requires `assignedTo`/`assigneeId`. Unassigned creates land as `OPEN` or `DOCUMENT_PENDING`; assign checks (`PermAssign` + `CanAssignTo`) only when an assignee is provided. Employees still lack `work.create`. Assignee can be set later via AssignSlot. Regression: `TestCreateWork_UnassignedOpen`. |
| **Regression** | `TestCreateWork_UnassignedOpen` + `go test ./internal/workmgmt/...` PASS |

### QA checklist

- [x] Rebuild/restart API `:8080`
- [x] Manager `POST /work/items` with title + client only (no assignee) → **201**, status `OPEN`, empty assignee
- [x] CA create with `status: DOCUMENT_PENDING` and no assignee → succeeds (**201**)
- [x] Manager create **with** assignee still succeeds (**201**)
- [x] Employee create still rejected (**403**)
- [x] AssignSlot can fill assignee after unassigned create (**200**)
- [x] `go test ./internal/workmgmt/...` PASS

**Verdict:** VERIFIED. Developer may proceed to **BUG-0011**.

---
