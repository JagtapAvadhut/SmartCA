## BUG-0011

| Field | Value |
|-------|--------|
| **Title** | Practice / WM seed has no Receptionist in `@wm.smartca.in` org (reception only in demo USR seed) |
| **Severity** | Medium |
| **Priority** | P1 |
| **Status** | **VERIFIED** (2026-07-27) |
| **Steps** | Try `reception1@wm.smartca.in`; query roles. |
| **Expected** | Reception in practice org for intake desk UAT alongside Manager/CA/TL/Emp. |
| **Actual (original)** | No WM reception user. Demo `manoj.malhotra@smartca.in` / `harsh.bose@smartca.in` (`receptionist`) work for intake API/UI, but outside WM 5k org. |
| **Screenshot** | `saas/qa-artifacts/qa-manual/reception_intake.png` |
| **Video** | N/A |
| **API evidence (original)** | Reception login OK on demo user; intake create 201; work list total 0; assign/close 403. |
| **API evidence (retest)** | `reception1@wm.smartca.in` login **200** role=`reception` id=`WM-RCP-0001`; perms include `intake.create` (no `work.assign`/`work.close.manager`/`work.edit`); `GET /work/items` **200 total=0**; `POST /work/intakes` **201** (`019fa3c5-922e-7488-bbb5-b0a87cd84785`); assign **403**; close **403**. Artifacts: `saas/qa-artifacts/qa-manual/bug0011-*.json`. |
| **Database evidence (retest)** | `WM-RCP-0001` / `reception1@wm.smartca.in` role=`reception`; `WM-RCP-0002` / `reception2@wm.smartca.in` role=`reception` (plus demo USR-* and PRACTICE-RCP-*). |
| **Root Cause hypothesis** | `wmseed` only creates manager/ca/tl/employee. |
| **Fix (Developer)** | `wmseed` seeds `WM-RCP-0001/0002` (`reception1/2@wm.smartca.in`, role `reception`, `PermissionsForRole` intake-only, password `SmartCA@2025`, reportsTo first manager). One-shot `go run ./cmd/wmreceptionseed` upserts without wiping 5k works. `practiceuatseed` already has ROLE_HIERARCHY reception (Fatima/Leena). |
| **Regression** | `go test ./internal/workmgmt/...` PASS |

### QA checklist

- [x] Login `reception1@wm.smartca.in` / `SmartCA@2025` → **200**, role=`reception`
- [x] Work list total **0**
- [x] Intake create allowed → **201**
- [x] Assign → **403**; Close → **403**
- [x] `go test ./internal/workmgmt/...` PASS

**Verdict:** VERIFIED. Developer may proceed to **BUG-0012**.

---
