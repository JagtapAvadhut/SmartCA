## BUG-0006

| Field | Value |
|-------|--------|
| **Title** | Load seed leaves ownership triad empty on nearly all work items |
| **Severity** | High |
| **Priority** | P0 |
| **Status** | **VERIFIED** (2026-07-27) |
| **Steps** | 1. Open any seeded work in UI `/work/{id}`. 2. Query DB for `owner_ca_id` / `tl_id`. |
| **Expected** | Practice Core works have Owner CA + TL + Assignee triad populated (BC-P0-11). |
| **Actual (original)** | UI shows OWNER CA `—`, TEAM LEADER `—`; only ASSIGNEE set. DB: **5006** rows missing triad; **3** with both owner+tl (QA-created). |
| **Screenshot** | `saas/qa-artifacts/qa-manual/manager_work_detail.png` (original; UI retest optional — API+DB clear) |
| **Video** | N/A |
| **API evidence (retest)** | GET `019fa328-f1f9-7227-b1b5-2cc7275ca9f2` → `ownerCaId=WM-CA-0006`, `tlId=WM-TL-0026`, `assigneeId=WM-TL-0026`. GET `PRACTICE-WRK-0001` → `ownerCaId=PRACTICE-CA-VAMSI`, `tlId=PRACTICE-TL-AMIT`. List samples also populated. |
| **Database evidence (before)** | `triad_missing=5006`, `with_triad=510` (incl. 500 PRACTICE + QA). |
| **Database evidence (retest)** | `triad_missing=0`, `with_triad=5516` (all non-deleted); active (excl. CANCELLED/CLOSED): `triad_missing=0`, `with_triad=4653`. Migration `009_ownership_triad_backfill` applied. |
| **Root Cause hypothesis** | `wmseed` predates Practice Core columns; never backfilled triad after 007. |
| **Fix (Developer)** | Migration `009_ownership_triad_backfill` (role-aware + WM-* pool heuristic); `wmseed` inserts triad; `CreateWork`/`ApplyOwnershipTriadDefaults` derive owner/tl from actor+assignee roles. Regression: `TestCreateWork_PopulatesOwnershipTriad`. |

### Fix details

- **009** backfill heuristics: H0 assignee sync; H1 CA/TL from assigner/assignee roles (users + id prefixes); H2 engagement owner; H3 WM-* deterministic `WM-CA-((n-1)%20)+1` / `WM-TL-((n-1)%50)+1` from assignee digits.
- PRACTICE-* already had triad from `practiceuatseed` (500/500); left intact.
- New creates: CA actor → `ownerCaId`; TL actor → `tlId`; CA/TL assignee role fills the matching slot when empty.

### QA checklist

- [x] `triad_missing` near 0 on local/staging after 009
- [x] Detail/API shows Owner CA + Team Leader for seeded WM work (`ownerCaId`/`tlId` present)
- [x] `go test ./internal/workmgmt/...` PASS
- [x] New create triad covered by `TestCreateWork_PopulatesOwnershipTriad` PASS

---
