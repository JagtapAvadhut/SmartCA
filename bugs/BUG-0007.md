## BUG-0007

| Field | Value |
|-------|--------|
| **Title** | WM hierarchy has no `reports_to`; DownlineIDs not hydrated — CA/TL scope incomplete |
| **Severity** | High |
| **Priority** | P0 |
| **Status** | **FIXED-PENDING-QA** |
| **Steps** | Inspect WM user JSON for `reportsTo`/`reports_to`; login as CA and compare list totals to Architecture downline scope. |
| **Expected** | Manager places hierarchy; CA list = portfolio + downline; TL = team + downline (Architecture / Backend gap note). |
| **Actual (original)** | **0** WM users with reports_to fields. CA list total **254**, TL **50**, Emp **10** — appears assignee/seed partitioning only, not org tree. Backend_IMPLEMENTATION lists hydration as Partial. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence (original)** | `GET /work/items` totals by role as above. |
| **Database evidence (original)** | `with_reports_to=0` for `WM-%` users. |
| **Database evidence (fix)** | Migration `010_wm_reports_to_backfill`: WM `with_reports_to=370` / 375 (5 managers root). Sample: `WM-CA-0001→WM-MGR-0001`, `WM-TL-0001→WM-CA-0001`, `WM-EMP-0001→WM-TL-0001`. |
| **Root Cause hypothesis** | Seed + auth middleware never wire `reports_to` → `Actor.DownlineIDs`. |
| **Fix (Developer)** | `wmseed` writes Manager→CA→TL→Emp `reportsTo`/`reports_to`; migration 010 backfills existing WM users; `WorkHandler.DownlineFn` + `CollectDownlineIDs` hydrate `Actor.DownlineIDs` on every work request; CreateTeamUser sets reports_to to creator; practiceuatseed already seeded tree (roster assert added). Tests: `downline_scope_test.go`. |

### Fix details

- Hierarchy pools: CA→MGR `((n-1)%5)+1`, TL→CA `((n-1)%20)+1`, Emp→TL `((n-1)%50)+1`.
- List/dashboard scope already used `ScopeDownlineIDs` via `applyListScope`; hydration was the missing piece.
- PRACTICE-* roster retained existing `reportsTo` edges; printCounts now logs reports_to coverage.

### QA checklist

- [ ] WM users `with_reports_to` ≈ 370 (managers empty OK)
- [ ] CA `GET /work/items` total reflects portfolio + downline (not assignee-partition only)
- [ ] TL list includes team (`tl_id`) + downline assignees
- [ ] `go test ./internal/workmgmt/...` PASS
- [ ] New create-team-user gets `reportsTo` = creator

---
