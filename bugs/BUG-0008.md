## BUG-0008

| Field | Value |
|-------|--------|
| **Title** | Auth login JWT/user.permissions omit Practice Core permission strings |
| **Severity** | Medium |
| **Priority** | P1 |
| **Steps** | Login Manager / TL / CA; inspect `data.user.permissions`. |
| **Expected** | Token/user payload includes `work.transition`, `work.verify.tl`, `work.verify.ca`, `work.close.manager`, `intake.*`, etc., or docs state role-derivation is sole source. |
| **Actual** | WM Manager perms list classic WM only (view/create/edit/delete/assign/…). Missing practice gate perms. API still allows gates via `PermissionsForRole` fallback; FE merges derived perms in `permissions.ts`. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence** | `jwt_includes_practice_perms FAIL missing=work.transition,work.verify.tl,work.verify.ca,work.close.manager,work.reopen,intake.create,intake.approve`. |
| **Database evidence** | N/A (user `data.permissions` JSON stale vs hierarchy). |
| **Root Cause hypothesis** | Seeded user permission arrays not regenerated for Practice Core; dual source of truth. |
| **Status** | FIXED-PENDING-QA |
| **Fix** | `sanitizeUser` (login + `/auth/me` via `UserFromSession`) unions stored permissions with `workmgmt.PermissionsForRole(NormalizeHierarchyRole(role))`. Manager SoD preserved (no `work.verify.tl`/`work.verify.ca`/`work.close.partner`). `wmseed` now seeds via `PermissionsForRole`; FE `hierarchyWorkPermissions` aligned for manager. |

---
