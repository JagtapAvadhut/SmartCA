## BUG-0008

| Field | Value |
|-------|--------|
| **Title** | Auth login JWT/user.permissions omit Practice Core permission strings |
| **Severity** | Medium |
| **Priority** | P1 |
| **Status** | **VERIFIED** (2026-07-27) |
| **Steps** | Login Manager / TL / CA; inspect `data.user.permissions`. |
| **Expected** | Token/user payload includes `work.transition`, `work.verify.tl`, `work.verify.ca`, `work.close.manager`, `intake.*`, etc., or docs state role-derivation is sole source. |
| **Actual (original)** | WM Manager perms list classic WM only (view/create/edit/delete/assign/…). Missing practice gate perms. API still allows gates via `PermissionsForRole` fallback; FE merges derived perms in `permissions.ts`. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence (original)** | `jwt_includes_practice_perms FAIL missing=work.transition,work.verify.tl,work.verify.ca,work.close.manager,work.reopen,intake.create,intake.approve`. |
| **API evidence (retest)** | Rebuilt API `:8080` (`health/ready` OK). `manager1@wm.smartca.in`: has `work.transition`, `work.close.manager`, `intake.approve`; SoD OK (no `work.verify.tl` / `work.verify.ca`). `emp1@wm.smartca.in`: has `work.transition`; no `work.assign` / `intake.approve`. `tl1@wm.smartca.in`: has `work.verify.tl`. |
| **Database evidence** | N/A (user `data.permissions` JSON stale vs hierarchy — merge at login/`sanitizeUser`). |
| **Root Cause hypothesis** | Seeded user permission arrays not regenerated for Practice Core; dual source of truth. |
| **Fix (Developer)** | `sanitizeUser` (login + `/auth/me` via `UserFromSession`) unions stored permissions with `workmgmt.PermissionsForRole(NormalizeHierarchyRole(role))`. Manager SoD preserved (no `work.verify.tl`/`work.verify.ca`/`work.close.partner`). `wmseed` now seeds via `PermissionsForRole`; FE `hierarchyWorkPermissions` aligned for manager. |
| **Regression** | `TestLogin_MergesPracticeCorePermissions` PASS; `go test ./internal/workmgmt/` PASS. |

### QA checklist

- [x] Rebuild/restart API `:8080`
- [x] Manager login includes practice gates + SoD (no TL/CA verify)
- [x] Emp login includes `work.transition`; excludes assign/intake.approve
- [x] TL login includes `work.verify.tl`
- [x] Auth merge + workmgmt tests PASS

**Verdict:** VERIFIED. Developer may proceed to **BUG-0009**.

---
