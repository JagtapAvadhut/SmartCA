## BUG-0004

| Field | Value |
|-------|--------|
| **Title** | Duplicate period work (`workType`+`company`+`periodKey`) returns 500 not conflict |
| **Severity** | High |
| **Priority** | P1 |
| **Status** | **VERIFIED** (2026-07-27 QA Regression) |
| **Steps** | Manager creates two works: same `workType:GSTR1`, `clientId:CLT-0001`, `companyId:CMP-0001`, `periodKey:2099-01`, with assignee. |
| **Expected** | **409 Conflict** (or clear 400) citing unique period constraint (Architecture / DB-P1-07). |
| **Actual (original)** | First **201**, second **500** `failed to create work`. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence (original)** | `period unique 201 500 {"code":"INTERNAL_ERROR","message":"failed to create work"}`. |
| **Database evidence (original)** | Unique indexes from migration 007 on period keys. |
| **Root Cause hypothesis** | Unique violation not translated to `ErrConflict` / API conflict mapper. |
| **Fix (Developer)** | Map Postgres `23505` on `uq_wm_work_company_period` / `uq_wm_work_client_period` to `Conflict` (409). Regression: `TestCreateWork_DuplicatePeriod_Conflict`. |

### QA Verification (2026-07-27)

| Check | Result |
|-------|--------|
| Rebuild API (`Go/bin/api.exe`) + restart on `:8080` | Done |
| `go test ./internal/workmgmt/... -run TestCreateWork_DuplicatePeriod_Conflict -v` | **PASS** (company + client period) |
| `go test ./internal/workmgmt/...` | **PASS** |
| Live: `periodKey:2087-11` (fresh) first create | **201** id `019fa39b-61ed-7022-bba0-a783ecedd1e7` |
| Live: same payload second create | **409** `CONFLICT` `"work already exists for this company, workType, and periodKey"` (not 500) |
| Live: `periodKey:2088-04` first/second | **201** then **409** (same message) |
| Live: `periodKey:2099-01` (pre-existing row) | **409** Conflict (not 500) |
| DB | One row each for `2087-11`, `2088-04`, `2099-01` (`GSTR1`/`CMP-0001`) |

**Verdict:** VERIFIED. Developer may proceed to BUG-0005.

---
