## BUG-0009

| Field | Value |
|-------|--------|
| **Title** | Dashboard `completed`/`completedTasks` ignores practice statuses (only CLOSED) |
| **Severity** | Medium |
| **Priority** | P1 |
| **Status** | **VERIFIED** (2026-07-27) |
| **Steps** | Manager `GET /work/dashboard` after 007 backfill (833 `READY_FOR_MANAGER_CLOSE` from legacy completed). |
| **Expected** | Dashboard reflects practice reality (close queue vs closed) or documents legacy mapping; not silently `completed:0/1` while hundreds await close. |
| **Actual (original)** | `completed:1`, `completedTasks:1`, `pending:4172` while DB has 833 `READY_FOR_MANAGER_CLOSE` and only ~2 CLOSED. FE dashboard still legacy widgets (FRONTEND_IMPLEMENTATION gap). |
| **Screenshot** | `saas/qa-artifacts/qa-manual/manager_work_dashboard.png` |
| **Video** | N/A |
| **API evidence (original)** | Dashboard JSON as above. |
| **API evidence (retest)** | Rebuilt API `:8080`. Manager `GET /api/v1/work/dashboard`: `completed=30`, `completedTasks=30`, `pending=4653`, `pendingTasks=4653`, `readyForTLVerify=874`, `readyForCAVerify=50`, `readyForManagerClose=851`, `awaitingClose=851`. Artifact: `saas/qa-artifacts/qa-manual/bug0009-dashboard.json`. |
| **Database evidence (retest)** | `CLOSED=30`, `READY_FOR_MANAGER_CLOSE=851`, pending excl CLOSED/CANCELLED=`4653` (matches API). Histogram also: TL_VERIFY=874, CA_VERIFY=50, CANCELLED=833, total=5516. |
| **Root Cause hypothesis** | Dashboard SQL still counts legacy `completed` / only `CLOSED`, not practice queues. |
| **Fix (Developer)** | Practice-aware dashboard aggregates: `completed`/`completedTasks` = CLOSED (+ legacy `completed`); `pending`/`pendingTasks` = not CLOSED/CANCELLED; expose `readyForTLVerify`, `readyForCAVerify`, `readyForManagerClose`, `awaitingClose` (alias). Postgres SQL + MemoryStore aligned. FE dashboard cards show verify/close queues. Regression: `TestDashboard_PracticeAwareAggregates`. |
| **Regression** | `TestDashboard_PracticeAwareAggregates` + `go test ./internal/workmgmt/...` PASS |

### Sample dashboard JSON fields (retest)

```json
{
  "pending": 4653,
  "completed": 30,
  "completedTasks": 30,
  "pendingTasks": 4653,
  "readyForTLVerify": 874,
  "readyForCAVerify": 50,
  "readyForManagerClose": 851,
  "awaitingClose": 851
}
```

### QA checklist

- [x] Rebuild/restart API `:8080`
- [x] Manager `GET /work/dashboard` → `completed` ≈ CLOSED count; `readyForManagerClose` / `awaitingClose` ≈ READY_FOR_MANAGER_CLOSE count
- [x] `pending` excludes CLOSED + CANCELLED
- [x] FE dashboard shows Awaiting Close / verify queue cards (code: `WorkDashboardPage.tsx`)
- [x] `go test ./internal/workmgmt/...` PASS

**Verdict:** VERIFIED. Developer may proceed to **BUG-0010**.

---
