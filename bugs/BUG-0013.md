## BUG-0013

| Field | Value |
|-------|--------|
| **Title** | Concurrent gate conflict / FOR UPDATE load test |
| **Severity** | Low |
| **Priority** | P2 |
| **Status** | **VERIFIED** (2026-07-27) |
| **Steps** | Attempt parallel verify/close on same work. |
| **Expected** | One 200, other 409 per ApplyGateWrite. |
| **Actual (before)** | Not tested in prior pass (time/tooling). Unit coverage claimed in DATABASE_P0_REMEDIATION; no live concurrent PG proof. |
| **Actual (after / QA retest)** | Concurrent proof PASS — unit (memory+postgres) and live API parallel close. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **Root Cause hypothesis** | Test gap only — `ApplyGateWrite` already enforces expected-status / `FOR UPDATE` conflict; missing reliable concurrent proof. |
| **Fix (Developer)** | Added `TestConcurrentGateWrite_Conflict` in `Go/internal/workmgmt/concurrent_gate_test.go` (MemoryStore + optional Postgres). |
| **Regression** | `go test ./internal/workmgmt/ -run TestConcurrentGateWrite_Conflict -count=1` + full `./internal/workmgmt/` PASS |

### Evidence (QA retest 2026-07-27)

**Unit / integration:**

```
go test ./internal/workmgmt/ -run TestConcurrentGateWrite_Conflict -count=1 -v
PASS — memory_ApplyGateWrite, memory_VerifyTL, memory_CloseWork,
       postgres_ApplyGateWrite, postgres_VerifyTL, postgres_CloseWork
       (postgres enabled: client=CLT-0139 company=CMP-0001)

go test ./internal/workmgmt/ -count=1
ok  github.com/JagtapAvadhut/smartca-backend/internal/workmgmt
```

**Live API parallel close (PowerShell jobs):**

- Actor: `manager1@wm.smartca.in` / `SmartCA@2025`
- Work: `PRACTICE-WRK-0393` (`READY_FOR_MANAGER_CLOSE`)
- Parallel `POST /api/v1/work/items/{id}/close`
- Result: **ok=1 conflict=1** → HTTP **200** + HTTP **409**
- Artifact: `saas/qa-artifacts/qa-manual/bug0013-parallel-close.json`

### QA checklist

- [x] `TestConcurrentGateWrite_Conflict` PASS (memory + postgres subtests)
- [x] Full `go test ./internal/workmgmt/ -count=1` PASS
- [x] Live parallel close → exactly one 200 and one 409

**Verdict:** VERIFIED. Developer may proceed to **BUG-0014**.

---
