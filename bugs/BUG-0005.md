## BUG-0005

| Field | Value |
|-------|--------|
| **Title** | Alok 55-user Practice UAT seed missing (BC-P0-13) |
| **Severity** | High |
| **Priority** | P0 |
| **Status** | **VERIFIED** (2026-07-27) |
| **Steps** | Query users for Alok / Nitesh / Mukesh named practice org; compare to ROLE_HIERARCHY / TEST_DATA_PLAN 55-user UAT. |
| **Expected** | Dedicated ~55 named practice users + ~300 clients / ~100 companies / realistic engagements for UAT (Architecture D5 / BC-P0-13). |
| **Actual (original)** | `COUNT` for Alok/Nitesh/Mukesh names = **0**. Only WM load seed: 5 managers / 20 CA / 50 TL / 300 emp + separate demo USR-* roles. Backend doc admits D5 deferred. |
| **Screenshot** | N/A |
| **Video** | N/A |
| **API evidence (retest)** | `POST /api/v1/auth/login` `identifier=alok@practice.smartca.in` / `SmartCA@2025` → **200**; user `PRACTICE-MGR-ALOK` / `Alok Joshi` / `role=manager` / `seed=practice_uat` / org `Alok Practice — Pune CA Firm`. |
| **Database evidence (retest)** | `fullName` ILIKE Alok/Nitesh/Mukesh → **1/1/1**. Named `PRACTICE-%` (excl pending) = **55**; +2 `PRACTICE-PEND-*` → **57** total. Clients **300** (golden ABC/Bright/CloudNine/Mehta/Zenith present), companies **100**, engagements **120**, works **500**, intakes **15**, HR-created works **0**. |
| **Root Cause hypothesis** | `wmseed` implements 5k load seed only; Practice UAT seed never shipped. |
| **Regression** | `go test ./cmd/practiceuatseed/` PASS; `go test ./internal/workmgmt/...` PASS. |

### Fix

- Added `Go/cmd/practiceuatseed` — idempotent Alok Practice UAT seeder (wipes only `PRACTICE-*` / `@practice.smartca.in`).
- Seeds **55** named users (Manager Alok, CAs Nitesh/Vamsi/Anudeep, TLs Mukesh/Amit/Sneha, seniors/juniors/articles/accountants, HR/Reception/Admin/Partners) + 2 `PENDING_PLACEMENT` placeholders.
- Seeds **300** clients (incl. golden ABC/Bright/CloudNine/Mehta/Zenith), **100** companies, **120** engagements, **500** practice-status works with triad, **15** intakes. HR-created works = 0.
- Password hash same as wmseed (`SmartCA@2025`). Smoke: `go test ./cmd/practiceuatseed/`.

### Run

```bash
cd Go
go run ./cmd/practiceuatseed
# or: go build -o practiceuatseed.exe ./cmd/practiceuatseed && ./practiceuatseed.exe
```

Verify: `SELECT COUNT(*) FROM users WHERE data->>'fullName' ILIKE '%alok%';` (expect > 0).

### QA Verdict

**VERIFIED.** Developer may proceed to **BUG-0006**.
