## BUG-0009

| Field | Value |
|-------|--------|
| **Title** | Dashboard `completed`/`completedTasks` ignores practice statuses (only CLOSED) |
| **Severity** | Medium |
| **Priority** | P1 |
| **Status** | **FIXED-PENDING-QA** |
| **Steps** | Manager `GET /work/dashboard` after 007 backfill (833 `READY_FOR_MANAGER_CLOSE` from legacy completed). |
| **Expected** | Dashboard reflects practice reality (close queue vs closed) or documents legacy mapping; not silently `completed:0/1` while hundreds await close. |
| **Actual (original)** | `completed:1`, `completedTasks:1`, `pending:4172` while DB has 833 `READY_FOR_MANAGER_CLOSE` and only ~2 CLOSED. FE dashboard still legacy widgets (FRONTEND_IMPLEMENTATION gap). |
| **Screenshot** | `saas/qa-artifacts/qa-manual/manager_work_dashboard.png` |
| **Video** | N/A |
| **API evidence (original)** | Dashboard JSON as above. |
| **Database evidence** | Status histogram: OPEN/BLOCKED/IN_PROGRESS/CANCELLED/READY_FOR_MANAGER_CLOSE/READY_FOR_TL_VERIFY ~833 each; CLOSED count tiny. |
| **Root Cause hypothesis** | Dashboard SQL still counts legacy `completed` / only `CLOSED`, not practice queues. |
| **Fix (Developer)** | Practice-aware dashboard aggregates: `completed`/`completedTasks` = CLOSED (+ legacy `completed`); `pending`/`pendingTasks` = not CLOSED/CANCELLED; expose `readyForTLVerify`, `readyForCAVerify`, `readyForManagerClose`, `awaitingClose` (alias). Postgres SQL + MemoryStore aligned. FE dashboard cards show verify/close queues. Regression: `TestDashboard_PracticeAwareAggregates`. |
| **Regression** | `go test ./internal/workmgmt/...` PASS |

### Sample dashboard JSON fields

```json
{
  "pending": 4171,
  "completed": 2,
  "completedTasks": 2,
  "pendingTasks": 4171,
  "readyForTLVerify": 833,
  "readyForCAVerify": 0,
  "readyForManagerClose": 833,
  "awaitingClose": 833,
  "overdue": 0,
  "todaysWork": 0,
  "myWork": 0
}
```

### QA checklist

- [ ] Rebuild/restart API `:8080`
- [ ] Manager `GET /work/dashboard` → `completed` ≈ CLOSED count; `readyForManagerClose` / `awaitingClose` ≈ READY_FOR_MANAGER_CLOSE count
- [ ] `pending` excludes CLOSED + CANCELLED
- [ ] FE dashboard shows Awaiting Close / verify queue cards
- [ ] `go test ./internal/workmgmt/...` PASS

---
